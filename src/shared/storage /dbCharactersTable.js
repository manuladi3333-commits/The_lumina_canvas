// Defines the "characters" table schema only.
// ++id = auto-incrementing primary key
// name, seed, createdAt = searchable/indexed fields
export const charactersSchema = "++id, name, seed, createdAt";